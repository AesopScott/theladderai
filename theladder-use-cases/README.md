# Deprecated Standalone Surface

This directory is deprecated as a standalone Use Cases surface, but it is not
dead code.

Use-case functionality is being consolidated into the unified Ladder experience,
but the migration is not complete and files here may still be used. Do not
delete or casually rewrite this directory. Maintain it when required, but do not
build new standalone surfaces here.
